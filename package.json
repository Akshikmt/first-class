{
  "name": "chakrabortys-first-classes-root",
  "version": "1.0.0",
  "description": "Root workspace orchestrator for Chakraborty's First Classes learning platform.",
  "private": true,
  "scripts": {
    "install-all": "npm install --prefix frontend && npm install --prefix backend",
    "frontend": "npm run dev --prefix frontend",
    "backend": "npm start --prefix backend",
    "dev": "concurrently \"npm run frontend\" \"npm run backend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
